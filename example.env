# ===============================
# the_board Environment Settings
# ===============================

# --- State layer (SQLite default) ---
STATE_BACKEND=sqlite
STATE_DB_PATH=./state/the_board_state.db

# --- Primary models ---
# Pick one main reasoning model
PRIMARY_LLM=llama3.1:8b
# Alternate: mistral:7b for faster runs

# Embedding model for search/index
EMBEDDING_MODEL=mxbai-embed-large:335m

# --- Ollama runtime tuning ---
# Context window (tokens)
# 4096 is safe for CPU 7–8B, bump to 8192 when needed
OLLAMA_NUM_CTX=4096

# Parallelism and threads
# Safe defaults for a Ryzen 3600 (6C/12T)
OLLAMA_NUM_PARALLEL=2
OLLAMA_NUM_THREADS=6
